apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: vision
  namespace: default
spec:
  runLatest:
    configuration:
      build:
        source:
          git:
            url: https://github.com/mchmarny/spring-cloud-gcp.git
            revision: s1p-demo
        volumes:
        - name: persistent-cache
          persistentVolumeClaim:
            claimName: jib-build-cache
        template:
          name: jib-maven
          arguments:
          - name: CACHE
            value: persistent-cache
          - name: IMAGE
            value: &image us.gcr.io/s9-demo/vision:1.5.2
          - name: DIRECTORY
            value: spring-cloud-gcp-samples/spring-cloud-gcp-vision-api-sample
      revisionTemplate:
        spec:
          container:
            image: *image
